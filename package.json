{
  "name": "oneplus",
  "version": "0.0.0",
  "description": "oneplus one invite grabber",
  "main": "server/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.9.0",
    "socket.io": "^1.1.0"
  }
}
